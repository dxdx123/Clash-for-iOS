import SwiftUI

struct MGSettingView: View {
    
    var body: some View {
        NavigationStack {
            Form {
                Section {
                    
                } header: {
                    Text("内核")
                }
                Section {
                    
                } header: {
                    Text("主题")
                }
                Section {
                    
                }
            }
            .navigationTitle(Text("设置"))
        }
    }
}
